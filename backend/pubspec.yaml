name: studioflow_backend
description: Backend multiempresa e offline-first do StudioFlow.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  bcrypt: ^1.2.0
  crypto: ^3.0.7
  dart_jsonwebtoken: ^3.4.1
  http: ^1.6.0
  postgres: ^3.5.12
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  uuid: ^4.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
